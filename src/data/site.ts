// PROVISÓRIO — domínio ainda não registrado/confirmado. Atualizar aqui e em
// astro.config.mjs (propriedade `site`) assim que o domínio definitivo
// existir — sitemap, canonical, OG e schema.org dependem desse valor.
export const SITE_URL = 'https://www.ozenergiasolar.com.br';

export const site = {
  url: SITE_URL,
  corTemaClaro: '#f7f5f0', // --papel
  corTemaEscuro: '#121212', // --umbra
} as const;
